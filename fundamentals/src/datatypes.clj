(ns datatypes
  (:gen-class))

(defn -main
  "First function"
  []
  (println "some datatypes in clojure"))


(defn Datatypes []
  (def a 1)
  (def b 1.5)
  (def c)
  (def d 0xffbc)
  (def e "hello")
  (def f nil)
  (def g true)
  (def h 'atom)
  ;; atoms, keywords, javatypes (byte, short, int, long float, doubles...) 
  
  )